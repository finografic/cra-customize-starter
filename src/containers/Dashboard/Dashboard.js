import React from 'react';
import { ButtonLink } from 'components/Button';

const Dashboard = () => (
  <section>
    <p>This is the dashboard.</p>
    <ButtonLink to="/" label="Back to Homepage" />
  </section>
);

export default Dashboard;
